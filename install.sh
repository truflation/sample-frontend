#!/bin/sh
yarn --frozen-lockfile
cat <<EOF > config.js
//generated by install.sh
const config = {
4: {
  'inflationAddress': '$1',
  'apiAddress': '$2',
  'chainName': 'Rinkeby',
  'testnet': true
}
}
EOF
